const jsonWebToken = require('jsonwebtoken');

const authService = require('../services/auth-service');
const authController = require('auth-controller');

describe('The authentication API module', () => {
  it('allows a user to sign in', () => {

  });
});
